name: example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: none

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  merge_localizations:
    path: ../../../merge-localizations

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
